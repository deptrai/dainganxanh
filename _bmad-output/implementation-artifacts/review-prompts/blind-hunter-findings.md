- **src/actions/adminBookings.ts:246-249** — Malformed PostgREST filter on nested relation and countQuery missing join: `countQuery.eq('rooms.lots.id', filters.lotId)` and `query.eq('rooms.lots.id', filters.lotId)` attempt to filter on a 2-level nested relation via dot notation. In PostgREST, embedded filtering only applies to direct relationships (`rooms.lot_id`). Furthermore, `countQuery` selects only `'id'` (`select('id', { count: 'exact', head: true })`) without joining `rooms` or `lots`, causing PostgREST to return a 400 Bad Request whenever `filters.lotId` is provided.
- **src/actions/adminBookings.ts:250-254** — PostgREST filter injection and query breakage via unescaped search string: `searchTerm` is interpolated directly into raw PostgREST `.or(...)` filter clauses: `code.ilike.%${searchTerm}%,guest_name.ilike.%${searchTerm}%...`. PostgREST parses commas as condition separators and dots as operator/resource delimiters. Searching with commas, parentheses, quotes, or dots (such as an email address `john.doe@example.com` or city `Hanoi, Vietnam`) malforms the query syntax and causes a 400 query crash.
- **src/actions/adminBookings.ts:164-186** — Authorization bypass and broken tenant boundary for resort_manager: `verifyAdminRole()` authorizes `resort_manager`, but all database queries (`fetchAdminBookings`, `fetchAdminBookingDetail`, `confirmBooking`, `adminCancelBooking`) execute using `createServiceRoleClient()`, bypassing Row Level Security. The database migration specifically defines lot-scoped RLS policies for `resort_manager` via `admin_user_lots`, but this implementation never scopes queries by lot ID, allowing a resort manager to view, confirm, or cancel bookings across all other resorts and access PII organization-wide.
- **src/actions/adminBookings.ts:360-400** — Overbooking hazard from confirming expired reservations without conflict checks: `confirmBooking` only verifies that `status === 'pending'`. It does not verify whether `expires_at` has elapsed, nor does it check whether the room has already been reserved or booked by another guest for overlapping dates. Confirming an expired booking directly creates double bookings in production.
- **src/actions/adminBookings.ts:382-390** — Overwriting customer payment timestamp and failure to clear booking expiration: `confirmBooking` sets `payment_claimed_at: new Date().toISOString()`, destroying the timestamp recorded when the guest originally claimed payment. Moreover, unlike webhook confirmations, it fails to set `expires_at: null`, leaving confirmed bookings subject to expiration cron checks or stale countdown states.
- **src/actions/adminBookings.ts:382-395** — Silent race condition during status updates: Both `confirmBooking` and `adminCancelBooking` execute `.update(...)` with status preconditions (`.eq('status', 'pending')`), but neither inspects updated rows (no `.select('id')` checking returned count). If a booking was already cancelled or expired concurrently, PostgREST returns no error, and the action falsely returns success (`{}`) when zero rows were updated.
- **src/actions/adminBookings.ts:397-398** — Missing path revalidations for public marketing and customer views: `confirmBooking` and `adminCancelBooking` only revalidate `/crm/admin/bookings` paths. They do not revalidate the public availability routes (`/eco-tourism`, `/eco-tourism/[lotId]`), the guest's voucher page (`/eco-tourism/voucher/[code]`), or the customer booking list (`/crm/my-bookings`), leaving room availability and guest order statuses stale.
- **src/components/crm/BookingDetail.tsx:93-99** — Duplicate conflicting back buttons and customer portal URL leakage: `AdminBookingDetailClient` provides a back button to `/crm/admin/bookings`. However, the nested `<BookingDetail>` component unconditionally renders a second back button hardcoded to `/crm/my-bookings` because `hideCustomerActions` does not suppress it. An admin clicking the card back button is unexpectedly navigated to the customer CRM.
- **src/components/crm/BookingDetail.tsx:139-158** — Customer marketing card and booking link displayed on admin detail page: In `<BookingDetail>`, the expired booking notice ("Thời gian giữ chỗ cho đơn này đã hết hạn...") and its call-to-action button linking to `/eco-tourism` check only `{isPending && !isFuture}` without checking `hideCustomerActions`. Admins inspecting expired bookings see customer marketing banners and re-booking prompts.
- **src/components/admin/BookingFilterBar.tsx:1040-1047** — Empty search evaluation against undefined wipes deep-linked pagination on mount: `localFilters` is initialized with `useState(filters)`. When `filters.search` is `undefined`, `debouncedSearch` resolves to `''`. The effect condition `debouncedSearch !== filters.search` evaluates `'' !== undefined` as true, firing `onFiltersChange` after 300ms on every mount. In `AdminBookingClient`, `handleFilterChange` resets `page` to 1 and deletes `page` from the URL, destroying deep-linked pagination on initial render.
- **src/components/admin/AdminBookingClient.tsx:628-660** — Asynchronous search race conditions without request cancellation: `loadBookings` runs whenever `filters`, `page`, or `pageSize` changes without an AbortController or stale request guard. Rapid filter toggles or typing in search cause overlapping asynchronous server action calls, allowing slower earlier requests to overwrite newer query results.
- **src/components/admin/AdminBookingClient.tsx:716-724** — Dead code in AdminBookingTable empty state due to client component guard: `AdminBookingTable` contains an empty state UI with a `Bed` icon and action guidance. However, `AdminBookingClient` intercepts `bookings.length === 0` beforehand and renders a plain `<div>Không có đơn đặt phòng nào</div>`, rendering the empty state inside `AdminBookingTable` unreachable dead code.
- **src/components/admin/AdminBookingDetailClient.tsx:849-887** — Hidden error banner and missing dialog accessibility in cancellation modal: When `adminCancelBooking` fails, `showCancelModal` remains open while `error` is rendered in the background page behind the `fixed inset-0 bg-black/50` overlay. The modal lacks internal error display, does not trap focus, lacks ARIA attributes (`role="dialog"`, `aria-modal="true"`), and does not dismiss on `Escape` or backdrop click.
- **src/app/crm/admin/bookings/[bookingId]/page.tsx:478-504** — Duplicated query logic and orphaned `fetchAdminBookingDetail` server action: `AdminBookingDetailPage` re-implements user authentication, role checks, and raw Supabase SQL queries inline rather than invoking `fetchAdminBookingDetail(bookingId)`. As a result, `fetchAdminBookingDetail` in `src/actions/adminBookings.ts` is unused dead code in production.
- **src/components/admin/AdminBookingClient.tsx:686-698** — Destructive full-page error state prevents filter recovery and includes hardcoded emoji: When `error` is encountered, `AdminBookingClient` unmounts the header, filter bar, and table, replacing the screen with a red error card containing a hardcoded emoji icon. Because the filter controls are unmounted, users cannot adjust or reset an invalid search or date range that triggered the failure.
- **src/actions/adminBookings.ts:256-258** — Unchecked countQuery error causes silent pagination breakdown: In `fetchAdminBookings`, `const { count } = await countQuery` ignores `error` from `countQuery`. If `countQuery` fails, `count` defaults to 0 while `query` may still return rows, producing `totalPages: 0` and hiding pagination controls when records exist.
- **src/app/crm/admin/bookings/page.tsx:596** — Negative page query parameter generates invalid PostgREST range: `Number(page) || 1` evaluates to negative numbers if `?page=-1` is supplied. Passing `-1` to `fetchAdminBookings` yields `.range(-40, -21)`, triggering an unhandled HTTP 400 Range Not Satisfiable error from PostgREST.
- **src/actions/__tests__/adminBookings.test.ts:39-150** — Ineffective unit tests with unasserted mock chains and zero filter coverage: The test helper `makeQueryChain` resolves dummy data regardless of query parameters. None of the tests inspect payloads passed to `.update()`, none test error rejection paths or role variations (`super_admin`, `resort_manager`), and none test any of the five query filters or pagination arguments, masking syntax errors in database queries.
