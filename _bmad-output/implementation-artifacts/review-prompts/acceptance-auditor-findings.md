- **Back button navigates to wrong destination on admin detail** — violates **AC #1 / UX intent**: `BookingDetail` always links back to `/crm/my-bookings` (`src/components/crm/BookingDetail.tsx:93-99`) even when rendered in admin view. Evidence: `AdminBookingDetailClient` provides its own back link to `/crm/admin/bookings`, but the nested `BookingDetail` card also contains a back link hardcoded to `/crm/my-bookings`.
- **Search by SĐT/email advertised but not in AC** — violates **AC #2**: Filter search placeholder says "Mã, tên khách, SĐT, email", but AC only specifies "booking code or guest name". Search filter does include `guest_phone` and `guest_email`, but the UI label over-promises if these are untested.
- **`payment_claimed_at` not set on confirm as specified** — violates **AC #3**: `confirmBooking` in `src/actions/adminBookings.ts:229` sets `payment_claimed_at: new Date().toISOString()`. This is correct per AC but clobbers any existing value; AC does not require preserving customer-claimed timestamp, but blind-hunter notes this may conflict with webhook flow.
- **Room availability not verified before confirm** — violates **AC #3**: The action checks `status === 'pending'` only and does not validate `expires_at` or check overlapping bookings before confirming.
- **Cancel reason auto-filled instead of required** — violates **AC #4**: `adminCancelBooking` uses `const cancellationReason = reason || 'Admin hủy đặt phòng'` (line 270), meaning UI does not enforce reason entry. AC says "enter a cancellation reason".
- **Resort manager access is global, not lot-scoped** — violates **Task 1 / AC #5**: `resort_manager` is allowed globally by `verifyAdminRole` (`src/actions/adminBookings.ts:7`), but the spec requires scoping via `admin_user_lots` table.
- **Impersonation context not explicitly handled** — violates **AC #6**: Admin detail/list pages use `createServerClient().auth.getUser()` (real admin), so impersonation works only because it is ignored; no explicit support or test confirms an admin sees all bookings while impersonating.
- **Component and E2E tests missing** — violates **Tasks 7 & 8**: No `AdminBookingTable`, `BookingFilterBar`, page, or E2E specs were added; only `adminBookings.test.ts` exists.
